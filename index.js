function cpu(tarefa) {
    var tarefa = this.tarefa;
    var makespam ;
    function constructor() {
      for (var i = 0; i < count; i++) {
        this[makespam[i]] = this.tarefa[i];
      }
    }
    return constructor;
  }
  
  var machine = cpu();
  var row = new machine(1, 'john', 'au');
  console.log(); // displays: john